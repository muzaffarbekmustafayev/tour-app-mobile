node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# ── Muhit o'zgaruvchilari (MAXFIY) ───────────────────────────
# ⚠️  Bu fayllarni HECH QACHON commit qilmang!
.env
.env.local
.env*.local
# ✅ .env.example COMMIT qilinadi — namuna sifatida
!.env.example

# Native build papkalari (prebuild / expo run natijasi)
android/
ios/

# macOS
.DS_Store
